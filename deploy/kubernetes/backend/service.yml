apiVersion: v1
kind: Service
metadata:
  name: garment-backend-service
  namespace: garment-${ENVIRONMENT}
spec:
  selector:
    app: garment-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP