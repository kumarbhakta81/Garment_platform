{
  "name": "garment_platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "test:integration:staging": "echo 'Running integration tests against staging environment' && exit 0",
    "test:e2e:production": "echo 'Running end-to-end tests against production environment' && exit 0"
  }
}